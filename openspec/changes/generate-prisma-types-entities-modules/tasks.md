## 1. 生成器基础搭建

- [x] 1.1 新增 Prisma schema 解析入口，读取 `prisma/schema.prisma` 并构建模型元数据结构
- [x] 1.2 定义字段类型映射规则（标量、可空、列表、JSON、时间类型）并补充单元测试
- [x] 1.3 建立模板渲染基础能力，支持按模型输出 types、entities、modules 三类文件

## 2. 类型与实体生成

- [x] 2.1 为每个 Prisma 模型生成 TypeScript 类型文件并统一导出索引
- [x] 2.2 为每个模型生成实体类文件，覆盖标量字段和可选字段映射
- [x] 2.3 实现关系字段生成策略（外键/简化关系对象）并处理循环依赖导入

## 3. 模块骨架生成

- [x] 3.1 按模型生成 Nest 模块文件，遵循统一命名与目录规范
- [x] 3.2 增加可选生成项（service/controller）并提供默认模板
- [x] 3.3 实现幂等写入机制，确保重复执行不产生无意义差异

## 4. 命令与开发体验

- [x] 4.1 提供统一命令入口（如 `pnpm` script），支持全量与按模型增量生成
- [x] 4.2 实现 `--dry-run` 预览模式，输出待生成文件和差异摘要
- [x] 4.3 实现覆盖控制开关（默认保护手写文件，显式覆盖才允许写入）

## 5. 验证与文档

- [x] 5.1 以当前 schema 执行一次全量生成并提交基线产物
- [x] 5.2 编写/更新使用文档，说明 schema 变更后的标准流程和常见问题
- [x] 5.3 增加 CI 校验（至少包含生成一致性检查或 dry-run 检查）
