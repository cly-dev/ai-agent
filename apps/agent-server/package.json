{
  "name": "@omnix/agent-server-legacy",
  "version": "0.0.1",
  "private": true,
  "description": "迁移期别名：业务代码暂留仓库根目录 src/",
  "scripts": {
    "build": "pnpm --dir ../.. run build",
    "dev": "pnpm --dir ../.. run start:dev",
    "start": "pnpm --dir ../.. run start:prod",
    "lint": "pnpm --dir ../.. run lint"
  }
}
