DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_agent

JWT_SECRET=dev-jwt-secret
JWT_EXPIRES_IN=7d

REDIS_URL=redis://localhost:6379
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=123456
REDIS_DB=0

MEMORY_USER_TTL_SECONDS=0
MEMORY_SESSION_TTL_SECONDS=604800

APP_CLIENT_HOST=https://admin.a-premium-test.com

# Intent recall + embedding → 已写入 DB（LlmModelConfig transformers_embedding / IntentRecallConfig）
# 管理: GET/PUT /llm-model-config ，重启服务后生效
# AGENT_INTENT_RECALL_MODE=auto
# AGENT_EMBEDDING_LOCAL_MODEL=...

# 工作记忆：每轮结束后 LLM 刷新（失败则规则 merge 降级）
AGENT_WORKING_MEMORY_MODE=refresh